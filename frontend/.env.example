VITE_API_BASE_URL=http://localhost:3000/api

# Set this to "false" to switch from the local mock data to your actual backend server.
# Set this to "true" (or leave it empty) to continue using the embedded seed demo data.
VITE_USE_MOCK_API=false
